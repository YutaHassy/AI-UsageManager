# vsix に入れないもの。
#
# ここに書いたものは配布物から落ちます。逆に言うと、書かれていない .js は
# そのまま利用者の手元へ届きます (この拡張はビルド工程を持たないため、
# 展開したものが動いているものです)。
#
# ※ このファイルは vsix の中身から再構成したものです (原本は vsix に
#    含まれていません)。

# 梱包スクリプト自身。配布物ではなく、配布物を作る手順。
build_vsix.py
.vscodeignore

# Python の副産物。backend/ を丸ごとコピーするので、混ざると vsix が太る。
**/__pycache__/**
**/*.pyc
**/*.pyo

# 開発用
.vscode/**
.vscode-test/**
node_modules/**
.gitignore
.git/**
**/*.map
**/.DS_Store
